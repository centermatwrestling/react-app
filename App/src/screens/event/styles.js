import {
  StyleSheet
} from 'react-native'

const styles = {
  nameBar: {
    flex:1,
    flexDirection: 'row'
  },
  name: {
    flex: 1,
    textAlign:'center',
    marginTop: 20,
    fontSize:13,
    fontWeight: "bold"
  },
  row: {
    flex:1,
    flexDirection:'row',
    marginTop:30
  },
  rowItem: {
  },
  rowItemHeader: {
    fontWeight:'bold',
    marginBottom:-20
  }
}

export default styles
