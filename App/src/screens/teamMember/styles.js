import {
  StyleSheet
} from 'react-native'

const styles = {
  row: {
    flex:1,
    flexDirection:'row',
    marginTop:30
  },
  rowItem: {
  },
  rowItemHeader: {
    fontWeight:'bold'
  }
}

export default styles
